import React from "react";
import { Heading } from "spectacle";

export const SlideHeading = (props) => {
  const style = {
    titleStyles: {
      textAlign: "left",
      textColor: "#E0DFD5",
      size: 6
    }
  };
}
